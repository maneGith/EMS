{% extends 'base.html.twig' %}

{% block body %}
    <h1>Depeche</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depeche.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('depeche_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('depeche_edit', { 'id': depeche.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
