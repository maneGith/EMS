{% extends 'base.html.twig' %}

{% block body %}
    {{ include('includes/header.html.twig') }}

<div id="container" style="border: solid 1px #d2d2d2;overflow-x: hidden;overflow-y:hidden">
    <div class="row">

        <div class="column column-left menu">
            {{ include('includes/admin.html.twig') }}
        </div>
        <div class="column column-middle" style="background: #8E998F">

            <div style="background: #fff">
            <div>
                <span style="color:#337ab7;font-weight: bold">Paramètres généraux d'un tarif national</span>

            </div><br>

    {{ form_start(edit_form) }}
            <table class="table table-condensed">
                <tbody>


                <tr>
                    <th>Poids</th>
                    <td> {{ bareme.poidsmax }} <span class="rs-e">Modifier</span>
                        <div class="De-e">
                            <div class="form-group" style="max-width: 400px">

                                {{ form_widget(edit_form.poidsmax,{'attr':{'class':'form-control',
                                    'style':'border:solid 1px #337ab7'}}) }}
                                {{ form_errors(edit_form.poidsmax) }}
                            </div>
                            <span>
                                <input type="submit" value="Valider" class="btn btn-ems" />
                                <a href="{{ path('bareme_edit', { 'id': bareme.id }) }}" class="btn btn-ems">Annuler</a>
                            </span>
                        </div>
                    </td>
                </tr>

                <tr>
                    <th>Tarif</th>
                    <td> {{ bareme.tarif }} <span class="rs-e">Modifier</span>
                        <div class="De-e">
                            <div class="form-group" style="max-width: 400px">

                                {{ form_widget(edit_form.tarif,{'attr':{'class':'form-control',
                                    'style':'border:solid 1px #337ab7'}}) }}
                                {{ form_errors(edit_form.tarif) }}
                            </div>
                            <span>
                                <input type="submit" value="Valider" class="btn btn-ems" />
                                <a href="{{ path('bareme_edit', { 'id': bareme.id }) }}" class="btn btn-ems">Annuler</a>
                             </span>
                        </div>
                    </td>
                </tr>

                <tr>
                    <td colspan="2">
                         <span  style="float: right">
                    <ul>
                        <li style="float:left;">
                            <a href="{{ path('bareme_national') }}"><input type="button" value="Lister" class="btn  btn-ems" style="margin-right: 15px;"/></a>
                        </li>

                        <li style="float:left;">
                            <a href="{{ path('bareme_delete', { 'id': bareme.id }) }}" class="btn btn-ems">Suppr</a>
                        </li>
                    </ul>
                 </span>
                    </td>
                </tr>

                </tbody>
            </table>




            {{ form_end(edit_form) }}


            </div>
    </div>


</div>
</div>
{% endblock %}
