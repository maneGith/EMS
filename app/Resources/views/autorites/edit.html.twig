{% extends 'base.html.twig' %}

{% block body %}
    {{ include('includes/entete.html.twig') }}
    
    <!-- Menu and Content -->
    <div class="div-preview">
        <div class="d-flex" id="wrapper">
            <!-- Page menu -->
            {{ include('includes/menu.html.twig') }}
            
            <!-- Page Content -->
            <div id="page-content-wrapper">

                <div class="titre lien-titre" id="titre">
                    <span style="font-weight: bold">Nom et titre d'une autorité</span>            
                </div> 

                <div class="content" id="content">       
                   
                     {{ form_start(edit_form) }}
                        <table class="table table-condensed">
                            <tbody>

                            <tr>
                                <th>Nom</th>
                                <td>{{ autorite.nom }}       <span class="rs-e">Modifier</span>
                                    <div class="De-e">
                                        <div class="form-group" style="max-width: 400px">
                                            {{ form_widget(edit_form.nom,{'attr':{'class':'form-control',
                                                'style':'font-weight: bold;border:solid 1px #337ab7'}}) }}
                                            {{ form_errors(edit_form.nom) }}
                                        </div>
                                        <span>
                                            <input type="submit" value="Modifier" class="btn btn-ems" />
                                            <a href="{{ path('autorites_edit', { 'id': autorite.id }) }}" class="btn btn-ems">Annuler</a>
                                        </span>
                                    </div>
                                </td>
                            </tr>
                            <tr>
                                <th>Titre</th>
                                <td>{{ autorite.titre }}  <span class="rs-e">Modifier</span>
                                    <div class="De-e">
                                        <div class="form-group" style="max-width: 400px">
                                            {{ form_widget(edit_form.titre,{'attr':{'class':'form-control',
                                                'style':'font-weight: bold;border:solid 1px #337ab7'}}) }}
                                            {{ form_errors(edit_form.titre) }}
                                        </div>
                                    <span>
                                        <input type="submit" value="Modifier" class="btn btn-ems" />
                                        <a href="{{ path('autorites_edit', { 'id': autorite.id }) }}" class="btn btn-ems">Annuler</a>
                                    </span>
                                    </div>
                                </td>

                            </tr>




                            {{ form_end(edit_form) }}
                            <tr>
                                <td colspan="3">
                                <span  style="float: right">
                                        <ul>
                                            <li style="float:left;">
                                                <a href="{{ path('autorites_new') }}"><input type="button" value="Lister" class="btn  btn-ems" style="margin-right: 15px;"/></a>
                                            </li>

                                            <li style="float:left;">
                                                {{ form_start(delete_form) }}
                                                <a href="#"> <input type="submit" value="Suprm" class="btn  btn-ems"></a>
                                                {{ form_end(delete_form) }}
                                            </li>
                                        </ul>
                                </span></td>
                            </tr>
                            </tbody>
                        </table>
                    
                    
                </div> 
            </div>
        </div>
     </div>





{% endblock %}
